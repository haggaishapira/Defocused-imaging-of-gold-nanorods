function handles = set_default_general_settings(handles)    
    
    settings.shift_x = 0;
    settings.shift_y = 0;
    
    settings.default_first_t = 10;
    settings.default_last_t = inf;

    settings.plot_sequence = 0;

    settings.hour_scale = 0;

    handles.general_settings = settings;











